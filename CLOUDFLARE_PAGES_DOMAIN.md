# 🌐 Cloudflare Pages 域名配置

## ❌ 问题

访问链接显示 "潜在钓鱼网站" 警告，且域名是 `xxx.workers.dev`（Workers 域名）。

## ✅ 解决方案

### 问题分析

- `xxx.workers.dev` 是 **Cloudflare Workers** 的域名
- Cloudflare Pages 的域名应该是 `xxx.pages.dev`
- 如果看到 Workers 域名，可能是配置错误或使用了错误的服务

### 步骤 1: 确认 Pages 项目域名

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 进入 **Pages** → 选择你的项目
3. 在项目概览页面，查看 **Custom domains** 或 **Preview deployments**
4. 正确的 Pages 域名格式应该是：`xxx.pages.dev`

### 步骤 2: 获取正确的访问链接

在 Cloudflare Pages 项目中：

1. 点击 **Deployments** 标签
2. 找到最新的成功部署
3. 点击部署右侧的 **...** 菜单
4. 选择 **Visit site** 或查看 **Preview URL**

正确的链接应该是：
```
https://poker-assistant-xxx.pages.dev
```
或
```
https://xxx.pages.dev
```

### 步骤 3: 如果确实使用了 Workers

如果你确实创建了 Workers 项目而不是 Pages 项目：

1. **删除 Workers 项目**（如果不需要）
2. **创建新的 Pages 项目**：
   - 进入 **Pages** → **Create a project**
   - 选择 **Connect to Git**
   - 选择仓库和分支
   - 配置构建设置

### 步骤 4: 配置自定义域名（可选）

如果你有自己的域名：

1. 在 Pages 项目中，进入 **Custom domains**
2. 点击 **Set up a custom domain**
3. 输入你的域名（例如：`poker.yourdomain.com`）
4. 按照提示配置 DNS 记录

## 🔍 验证

访问正确的 Pages 域名，应该：
- ✅ 正常显示前端页面
- ✅ 没有安全警告
- ✅ 可以正常使用

## 📝 常见问题

### Q: 为什么会有 Workers 域名？

A: 可能的原因：
- 误创建了 Workers 项目
- Cloudflare 自动分配了 Workers 域名
- 配置混淆了 Pages 和 Workers

### Q: 如何区分 Pages 和 Workers？

- **Pages**: 用于静态网站，域名是 `xxx.pages.dev`
- **Workers**: 用于服务器端代码，域名是 `xxx.workers.dev`

### Q: 可以同时使用吗？

A: 可以，但通常：
- **Pages** 用于前端（React/Vue 等）
- **Workers** 用于 API 或后端逻辑

对于你的项目，应该使用 **Pages** 部署前端。

## 🎯 下一步

1. 确认并获取正确的 Pages 域名
2. 使用正确的域名访问前端
3. 配置前端环境变量指向后端 Tunnel URL
4. 测试完整功能

